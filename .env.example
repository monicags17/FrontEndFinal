# Environment Variables for UNKLAB Lost & Found

# EmailJS Configuration (for password reset emails)
# Get these values from https://www.emailjs.com/
# 1. Create an account at EmailJS
# 2. Add an email service (e.g., Gmail)
# 3. Create an email template for password reset
# 4. Copy the IDs below

VITE_EMAILJS_SERVICE_ID=your_service_id_here
VITE_EMAILJS_TEMPLATE_ID=your_template_id_here
VITE_EMAILJS_PUBLIC_KEY=your_public_key_here

# Note: If these are not configured, the app will work in development mode
# and show reset links in the browser console instead of sending emails.
